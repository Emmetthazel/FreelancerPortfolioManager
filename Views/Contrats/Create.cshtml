@model FreelancerPortfolioManager.Models.Contrat

@{
    ViewData["Title"] = "Créer un Contrat";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h3 class="mb-0">
                        <i class="fas fa-plus"></i> Créer un Nouveau Contrat
                    </h3>
                </div>
                <div class="card-body p-4">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger mb-3" role="alert"></div>
                    <form asp-action="Create" method="post">
                        @Html.AntiForgeryToken()

                        <div class="mb-3">
                            <label asp-for="Reference" class="form-label">
                                <i class="fas fa-hashtag"></i> Référence
                            </label>
                            <input asp-for="Reference" class="form-control" />
                            <span asp-validation-for="Reference" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="DateSignature" class="form-label">
                                <i class="fas fa-calendar-check"></i> Date de signature
                            </label>
                            <input asp-for="DateSignature" class="form-control" type="date" />
                            <span asp-validation-for="DateSignature" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Montant" class="form-label">
                                <i class="fas fa-euro-sign"></i> Montant
                            </label>
                            <input asp-for="Montant" class="form-control" type="number" step="0.01" />
                            <span asp-validation-for="Montant" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Conditions" class="form-label">
                                <i class="fas fa-file-alt"></i> Conditions
                            </label>
                            <textarea asp-for="Conditions" class="form-control" rows="6"></textarea>
                            <span asp-validation-for="Conditions" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Statut" class="form-label">
                                <i class="fas fa-info-circle"></i> Statut
                            </label>
                            <select asp-for="Statut" class="form-select" asp-items="Html.GetEnumSelectList<FreelancerPortfolioManager.Models.StatutContrat>()"></select>
                            <span asp-validation-for="Statut" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="ProjetId" class="form-label">
                                <i class="fas fa-project-diagram"></i> Projet
                            </label>
                            <select asp-for="ProjetId" class="form-select" asp-items="ViewBag.ProjetId">
                                <option value="">-- Sélectionner un projet --</option>
                            </select>
                            <span asp-validation-for="ProjetId" class="text-danger"></span>
                        </div>

                        <div class="d-grid gap-2 mt-4">
                            <button type="submit" class="btn btn-primary btn-lg">
                                <i class="fas fa-save"></i> Créer le Contrat
                            </button>
                        </div>

                        <div class="text-center mt-3">
                            <a asp-action="Index" class="btn btn-outline-secondary">
                                <i class="fas fa-arrow-left"></i> Retour à la liste
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

<style>
    .card {
        border-radius: 10px;
        overflow: hidden;
    }

    .btn-lg {
        padding: 12px 24px;
        font-size: 1.1rem;
    }
</style>
